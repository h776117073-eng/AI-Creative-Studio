import React, { useCallback, useEffect, useMemo, useRef, useState } from 'react';
import { AudioLines, Bot, Camera, Captions, ChevronDown, ChevronLeft, ChevronRight, CircleDot, Copy, Eye, EyeOff, Film, FolderOpen, Gauge, Headphones, Image as ImageIcon, Layers3, Lightbulb, Lock, Maximize2, MessageCircle, Mic2, Minus, Move3D, Music2, Pause, Play, Plus, Redo2, RotateCcw, Scissors, Settings2, SlidersHorizontal, Sparkles, Split, Star, Trash2, Type, Undo2, Upload, Volume2, VolumeX, Wand2, X, ZoomIn, ZoomOut, ChevronsDown, Download, FileInput, Save, GripVertical } from 'lucide-react';

type Category='media'|'templates'|'music'|'text'|'filters'|'effects'|'transitions'|'adjust'|'ai';
type Panel=Category|'assistant'|'settings'|'project'|'none';
type InspectorTab='clip'|'transform'|'color'|'audio'|'ai';
interface Asset{id:string;name:string;url:string;duration:number;mime:string;local?:boolean}
interface Clip{id:string;assetId:string;name:string;startTime:number;endTime:number;trimStart:number;trimEnd:number;duration:number;speed?:number;volume?:number;opacity?:number;rotate?:number;flipH?:boolean;flipV?:boolean;brightness?:number;contrast?:number;saturation?:number;grayscale?:boolean;fadeIn?:number;fadeOut?:number;effects?:string[];transition?:{type:string;duration:number};text?:string;fontSize?:number;color?:string}
interface Track{id:string;name:string;type:string;clips:Clip[];muted?:boolean;locked?:boolean;visible?:boolean;height?:number;order?:number}
interface Timeline{version?:number;duration:number;currentTime:number;tracks:Track[];markers?:any[]}
interface Project{id:string;name:string;timeline:Timeline;assets:Asset[];historyIndex:number;historyLength:number}
type Tool={id:string;label:string;icon:any;command?:string;property?:'brightness'|'contrast'|'saturation';open?:boolean};
const API=(import.meta.env.VITE_API_URL||'https://ai-creative-studio-api-0gl6.onrender.com').replace(/\/$/,'');
const api=(p:string)=>`${API}${p}`;const fmt=(n:number)=>{const m=Math.floor(n/60).toString().padStart(2,'0');const s=Math.floor(n%60).toString().padStart(2,'0');const c=Math.floor((n%1)*100).toString().padStart(2,'0');return `${m}:${s}:${c}`};const source=(a?:Asset)=>a?(a.local?a.url:api(a.url)):'';
const categories:Record<Category,{label:string;icon:any}>={media:{label:'الوسائط',icon:FolderOpen},templates:{label:'قوالب',icon:Film},music:{label:'موسيقى',icon:Music2},text:{label:'نص',icon:Type},filters:{label:'ملصقات',icon:Star},effects:{label:'تأثيرات',icon:Sparkles},transitions:{label:'انتقالات',icon:Move3D},adjust:{label:'ضبط',icon:SlidersHorizontal},ai:{label:'أدوات AI',icon:Bot}};
const tools:Record<Category,Tool[]>={media:[{id:'import',label:'استيراد',icon:Upload,open:true},{id:'duplicate',label:'تكرار',icon:Copy,command:'كرر المقطع'},{id:'snapshot',label:'لقطة',icon:Camera,command:'التقط صورة من الإطار الحالي'},{id:'freeze',label:'تجميد',icon:Minus,command:'جمّد الإطار عند المؤشر'}],templates:[{id:'cinematic',label:'سينمائي',icon:Film,command:'طبّق قالب سينمائي'},{id:'vlog',label:'Vlog',icon:Film,command:'طبّق قالب Vlog'},{id:'reels',label:'ريلز',icon:ImageIcon,command:'طبّق قالب ريلز'},{id:'promo',label:'إعلاني',icon:Star,command:'طبّق قالب إعلاني'}],music:[{id:'audio',label:'إضافة صوت',icon:Music2,open:true},{id:'record',label:'تسجيل',icon:Mic2,command:'سجّل تعليقًا صوتيًا'},{id:'enhance',label:'تحسين الصوت',icon:AudioLines,command:'حسّن الصوت'},{id:'noise',label:'تقليل الضوضاء',icon:Headphones,command:'قلّل ضوضاء الخلفية'}],text:[{id:'title',label:'عنوان',icon:Type,open:true},{id:'captions',label:'ترجمة تلقائية',icon:Captions,command:'أنشئ ترجمة تلقائية'},{id:'animated',label:'نص متحرك',icon:Sparkles,command:'أضف نصًا متحركًا'},{id:'lowerthird',label:'شريط سفلي',icon:Layers3,command:'أضف شريطًا سفليًا'}],filters:[{id:'sticker',label:'ملصق',icon:Star,command:'أضف ملصقًا'},{id:'emoji',label:'رموز',icon:CircleDot,command:'أضف رموزًا'},{id:'overlay',label:'تراكب',icon:Layers3,command:'أضف طبقة تراكب'},{id:'logo',label:'شعار',icon:Camera,command:'أضف شعارًا'}],effects:[{id:'blur',label:'تمويه',icon:Sparkles,command:'طبّق تمويه'},{id:'vignette',label:'تظليل',icon:Sparkles,command:'طبّق Vignette'},{id:'glow',label:'وهج',icon:Wand2,command:'أضف وهجًا'},{id:'grain',label:'حبيبات',icon:Sparkles,command:'أضف حبيبات فيلم'}],transitions:[{id:'fade',label:'تلاشي',icon:Move3D,command:'أضف انتقال تلاشي'},{id:'dissolve',label:'مزج',icon:Move3D,command:'أضف انتقال مزج'},{id:'zoom',label:'تكبير',icon:ZoomIn,command:'أضف انتقال تكبير'}],adjust:[{id:'brightness',label:'الإضاءة',icon:Lightbulb,property:'brightness'},{id:'contrast',label:'التباين',icon:SlidersHorizontal,property:'contrast'},{id:'saturation',label:'التشبع',icon:SlidersHorizontal,property:'saturation'},{id:'night',label:'ليلي سينمائي',icon:Sparkles,command:'اجعل الإضاءة ليلية سينمائية زرقاء'}],ai:[{id:'cut',label:'قص ذكي',icon:Scissors,command:'اقترح أفضل القصات'},{id:'silence',label:'حذف الصمت',icon:Scissors,command:'احذف فترات الصمت'},{id:'beats',label:'مزامنة الإيقاع',icon:Music2,command:'زامن القطع مع الإيقاع'},{id:'enhance',label:'تحسين الجودة',icon:Wand2,command:'حسّن جودة الفيديو'},{id:'camera',label:'حركة كاميرا',icon:Move3D,command:'أضف حركة كاميرا سينمائية'},{id:'tracking',label:'تتبع الحركة',icon:Move3D,command:'افتح تتبع الحركة'},{id:'reframe',label:'إعادة تأطير',icon:Maximize2,command:'أعد تأطير الفيديو تلقائيًا'},{id:'style',label:'تحليل الأسلوب',icon:Bot,command:'حلّل أسلوب الفيديو واقترح تحسينات'}]};
function ClipThumb({asset}:{asset?:Asset}){if(!asset)return <div className="h-full w-full bg-white/[.04]"/>;const s=source(asset);if(asset.mime.startsWith('image/'))return <img src={s} className="h-full w-full object-cover"/>;if(asset.mime.startsWith('video/'))return <video src={s} muted playsInline preload="metadata" className="h-full w-full object-cover"/>;return <div className="h-full w-full flex items-center justify-center bg-emerald-500/10"><Volume2 size={18}/></div>}
function Button({children,onClick,className='' }:{children:React.ReactNode;onClick?:()=>void;className?:string}){return <button onClick={onClick} className={`transition active:scale-[.98] ${className}`}>{children}</button>}
export function VireonMobileStudio({projectId}:{projectId:string}){
 const [project,setProject]=useState<Project|null>(null),[panel,setPanel]=useState<Panel>('none'),[tab,setTab]=useState<InspectorTab>('clip'),[selectedClip,setSelectedClip]=useState<string|null>(null),[playhead,setPlayhead]=useState(0),[playing,setPlaying]=useState(false),[zoom,setZoom]=useState(1),[status,setStatus]=useState('جاري تجهيز الاستوديو…'),[assistantText,setAssistantText]=useState(''),[messages,setMessages]=useState<{role:'user'|'assistant';text:string}[]>([]),[property,setProperty]=useState<'brightness'|'contrast'|'saturation'|null>(null),[propertyValue,setPropertyValue]=useState(0),[bottomTool,setBottomTool]=useState('قص'),[timelineHeight,setTimelineHeight]=useState(330);
 const fileRef=useRef<HTMLInputElement>(null),projectFileRef=useRef<HTMLInputElement>(null),videoRef=useRef<HTMLVideoElement>(null),audioRef=useRef<HTMLAudioElement>(null),projectRef=useRef<Project|null>(null),timelineRef=useRef<HTMLDivElement>(null),dragRef=useRef<{clipId:string;mode:'move'|'left'|'right';startX:number;startStart:number;startEnd:number}|null>(null);
 useEffect(()=>{projectRef.current=project},[project]);const tracks=project?.timeline.tracks||[],clips=tracks.flatMap(t=>t.clips||[]),active=clips.find(c=>c.id===selectedClip)||clips.find(c=>playhead>=c.startTime&&playhead<c.endTime)||clips.find(c=>c.assetId),activeAsset=project?.assets.find(a=>a.id===active?.assetId),duration=Math.max(.01,project?.timeline.duration||1),timelinePxPerSecond=Math.max(30,42*zoom),contentWidth=Math.max(720,duration*timelinePxPerSecond+140);
 useEffect(()=>{(async()=>{try{let id=projectId;if(id==='new'){const r=await fetch(api('/api/projects'),{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({name:'مشروع جديد'})});if(!r.ok)throw Error('تعذر إنشاء المشروع');const d=await r.json();id=d.id;history.replaceState({},'',`/project/${id}`)}const r=await fetch(api(`/api/projects/${id}`));if(!r.ok)throw Error('تعذر تحميل المشروع');const d=await r.json();setProject(d);projectRef.current=d;const first=(d.timeline?.tracks||[]).flatMap((t:Track)=>t.clips||[]).find((c:Clip)=>c.assetId);if(first){setSelectedClip(first.id);setPlayhead(first.startTime)}setStatus('جاهز')}catch(e){setStatus((e as Error).message)}})()},[projectId]);
 useEffect(()=>{const el=activeAsset?.mime.startsWith('audio/')?audioRef.current:videoRef.current;if(!el||!active)return;const t=Math.max(0,(active.trimStart||0)+playhead-active.startTime);try{if(Math.abs(el.currentTime-t)>.12)el.currentTime=Math.min(Number.isFinite(el.duration)?el.duration:t,t);el.playbackRate=active.speed||1;if(playing)void el.play();else el.pause()}catch{}},[playhead,active?.id,active?.startTime,active?.trimStart,active?.speed,activeAsset?.id,playing]);
 useEffect(()=>{if(!playing)return;const timer=window.setInterval(()=>setPlayhead(p=>{const n=p+0.04*(active?.speed||1);if(n>=duration){setPlaying(false);return 0}return n}),40);return()=>window.clearInterval(timer)},[playing,duration,active?.speed]);
 const saveTimeline=useCallback(async(next:Project)=>{setProject(next);projectRef.current=next;try{const r=await fetch(api(`/api/projects/${next.id}/timeline`),{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({timeline:next.timeline})});if(!r.ok)throw Error();setStatus('تم الحفظ')}catch{setStatus('تم الحفظ محليًا مؤقتًا')}},[]);
 const seek=(t:number)=>{const n=Math.max(0,Math.min(duration,t));setPlayhead(n);setProject(p=>p?{...p,timeline:{...p.timeline,currentTime:n}}:p)};
 const updateClip=(id:string,patch:Partial<Clip>)=>{const p=projectRef.current;if(!p)return;const next=structuredClone(p) as Project;for(const tr of next.timeline.tracks){const c=tr.clips.find(x=>x.id===id);if(c){Object.assign(c,patch);break}}void saveTimeline(next)};
 const command=async(text:string)=>{const p=projectRef.current;if(!p||!text.trim())return;setStatus('جارٍ تنفيذ الأمر…');setMessages(v=>[...v,{role:'user',text}]);try{const r=await fetch(api(`/api/projects/${p.id}/ai-command`),{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({text,clipId:active?.id,playhead})});const d=await r.json();if(!r.ok)throw Error(d.error||'تعذر تنفيذ الأمر');const next={...p,timeline:d.timeline};setProject(next);projectRef.current=next;setMessages(v=>[...v,{role:'assistant',text:d.command?.message||'تم التنفيذ'}]);setStatus(d.command?.message||'تم')}catch(e){const msg=(e as Error).message;setMessages(v=>[...v,{role:'assistant',text:msg}]);setStatus(msg)}};
 const importMedia=async(files:FileList|null)=>{if(!files?.length||!projectRef.current)return;for(const file of Array.from(files)){const local=URL.createObjectURL(file);setStatus(`جارٍ استيراد ${file.name}…`);const probe=document.createElement(file.type.startsWith('audio/')?'audio':'video');probe.preload='metadata';probe.src=local;const dur=await new Promise<number>(resolve=>{probe.onloadedmetadata=()=>resolve(Number.isFinite(probe.duration)?probe.duration:0);probe.onerror=()=>resolve(0)});try{const form=new FormData();form.append('file',file);const r=await fetch(api(`/api/projects/${projectRef.current!.id}/upload`),{method:'POST',body:form});if(!r.ok)throw Error();const remote=await r.json();setProject(remote);projectRef.current=remote;const c=remote.timeline.tracks.flatMap((t:Track)=>t.clips).filter((x:Clip)=>x.name===file.name).slice(-1)[0];if(c){setSelectedClip(c.id);seek(c.startTime)}}catch{const p=projectRef.current;const aid=`local-${crypto.randomUUID()}`,cid=`clip-${crypto.randomUUID()}`,type=file.type.startsWith('audio/')?'audio':'video',tr=p.timeline.tracks.find(t=>t.type===type)||p.timeline.tracks[0],start=type==='video'?p.timeline.duration:Math.max(0,...p.timeline.tracks.flatMap(t=>t.clips.map(c=>c.endTime)),0),asset:Asset={id:aid,name:file.name,url:local,duration:dur,mime:file.type,local:true},clip:Clip={id:cid,assetId:aid,name:file.name,startTime:start,endTime:start+dur,trimStart:0,trimEnd:dur,duration:dur,speed:1,volume:1,opacity:1,rotate:0,flipH:false,flipV:false,brightness:0,contrast:1,saturation:1,effects:[]},next={...p,assets:[...p.assets,asset],timeline:{...p.timeline,duration:type==='video'?start+dur:p.timeline.duration,tracks:p.timeline.tracks.map(t=>t.id===tr.id?{...t,clips:[...t.clips,clip]}:t)}};setProject(next);projectRef.current=next;setSelectedClip(cid);seek(start)}URL.revokeObjectURL(local)}setStatus('تم الاستيراد')};
 const exportProject=()=>{if(!project)return;const blob=new Blob([JSON.stringify({name:project.name,timeline:project.timeline,assets:project.assets.map(a=>({id:a.id,name:a.name,url:a.url,duration:a.duration,mime:a.mime}))},null,2)],{type:'application/json'});const a=document.createElement('a');a.href=URL.createObjectURL(blob);a.download=`${project.name||'vireon-project'}.vireon.json`;a.click();setTimeout(()=>URL.revokeObjectURL(a.href),1000)};
 const importProject=async(file:File|null)=>{if(!file||!projectRef.current)return;try{const payload=JSON.parse(await file.text());const r=await fetch(api('/api/projects/import'),{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify(payload)});if(!r.ok)throw Error('تعذر استيراد المشروع');const d=await r.json();setProject(d);projectRef.current=d;history.replaceState({},'',`/project/${d.id}`);setStatus('تم استيراد المشروع بالكامل')}catch(e){setStatus((e as Error).message)}};
 const split=()=>command(`قسّم عند ${playhead.toFixed(2)}`),duplicate=()=>command('كرر المقطع'),remove=()=>command('احذف المقطع');const openCategory=(c:Category)=>setPanel(p=>p===c?'none':c);const toolClick=(t:Tool)=>{if(t.open){if(t.id==='import'||t.id==='audio')fileRef.current?.click();else{const text=window.prompt('النص','عنوان جديد');if(text)void command(`أضف نص: ${text}`)}return}if(t.property){setProperty(t.property);setPropertyValue(t.property==='brightness'?(active?.brightness||0):t.property==='contrast'?Math.round((active?.contrast||1)*100):Math.round((active?.saturation||1)*100));return}if(t.command)void command(t.command)};
 const onTimelinePointerDown=(e:React.PointerEvent<HTMLDivElement>,clip:Clip,mode:'move'|'left'|'right')=>{e.preventDefault();e.currentTarget.setPointerCapture(e.pointerId);dragRef.current={clipId:clip.id,mode,startX:e.clientX,startStart:clip.startTime,startEnd:clip.endTime};setSelectedClip(clip.id)};
 useEffect(()=>{const move=(e:PointerEvent)=>{const d=dragRef.current;if(!d||!projectRef.current)return;const delta=(e.clientX-d.startX)/timelinePxPerSecond;const p=projectRef.current,next=structuredClone(p) as Project;for(const tr of next.timeline.tracks){const c=tr.clips.find(x=>x.id===d.clipId);if(!c)continue;const min=.08;if(d.mode==='move'){const span=d.startEnd-d.startStart,ns=Math.max(0,Math.min(duration-span,d.startStart+delta));c.startTime=ns;c.endTime=ns+span}else if(d.mode==='left'){const ns=Math.max(0,Math.min(c.endTime-min,d.startStart+delta));const dd=ns-d.startStart;c.startTime=ns;c.trimStart=Math.max(0,Math.min(c.trimEnd-min,c.trimStart+dd));c.duration=c.endTime-c.startTime}else{c.endTime=Math.max(c.startTime+min,Math.min(duration,d.startEnd+delta));c.trimEnd=Math.max(c.trimStart+min,c.trimEnd+delta);c.duration=c.endTime-c.startTime}break}setProject(next);projectRef.current=next};const up=()=>{if(dragRef.current&&projectRef.current)void saveTimeline(projectRef.current);dragRef.current=null};window.addEventListener('pointermove',move);window.addEventListener('pointerup',up);return()=>{window.removeEventListener('pointermove',move);window.removeEventListener('pointerup',up)}},[timelinePxPerSecond,duration,saveTimeline]);
 const rows=useMemo(()=>tracks.map(t=>({...t,clips:[...t.clips].sort((a,b)=>a.startTime-b.startTime)})),[tracks]);const setTrack=(id:string,patch:Partial<Track>)=>{const p=projectRef.current;if(!p)return;const next=structuredClone(p) as Project,tr=next.timeline.tracks.find(x=>x.id===id);if(tr)Object.assign(tr,patch);void saveTimeline(next)};const addTrack=()=>{const p=projectRef.current;if(!p)return;const next=structuredClone(p) as Project;next.timeline.tracks.push({id:crypto.randomUUID(),name:`مسار ${next.timeline.tracks.length+1}`,type:'video',clips:[],muted:false,locked:false,visible:true,height:64,order:next.timeline.tracks.length});void saveTimeline(next)};
 return <div dir="rtl" className="h-screen w-screen overflow-hidden bg-[#080a0f] text-white flex flex-col select-none"><input ref={fileRef} className="hidden" type="file" accept="video/*,audio/*,image/*" multiple onChange={e=>{void importMedia(e.target.files);e.currentTarget.value=''}}/><input ref={projectFileRef} className="hidden" type="file" accept="application/json,.json,.vireon.json" onChange={e=>{void importProject(e.target.files?.[0]||null);e.currentTarget.value=''}}/><style>{`.v-card{background:linear-gradient(180deg,rgba(24,27,36,.98),rgba(15,17,24,.98));border:1px solid rgba(255,255,255,.08);box-shadow:0 10px 30px rgba(0,0,0,.18)}.v-btn{background:rgba(255,255,255,.045);border:1px solid rgba(255,255,255,.08);border-radius:14px}.v-scroll::-webkit-scrollbar{width:5px;height:5px}.v-scroll::-webkit-scrollbar-thumb{background:rgba(255,255,255,.12);border-radius:99px}.timeline-bg{background:linear-gradient(to bottom,rgba(255,255,255,.035) 1px,transparent 1px);background-size:100% 64px}`}</style>
 <header className="h-[64px] shrink-0 px-3 sm:px-5 flex items-center justify-between border-b border-white/[.07] bg-[#0a0c11]/95 backdrop-blur-xl z-40"><div className="flex items-center gap-3"><div className="w-9 h-9 rounded-xl bg-gradient-to-br from-violet-600 to-fuchsia-500 flex items-center justify-center font-black">V</div><div className="text-lg font-black">Vireon</div><Button onClick={()=>setPanel(panel==='project'?'none':'project')} className="text-sm text-white/85 flex items-center gap-1">{project?.name||'مشروع جديد'} <ChevronDown size={15}/></Button></div><div className="flex items-center gap-1.5"><Button className="v-btn p-2" onClick={()=>{if(project){void fetch(api(`/api/projects/${project.id}/undo`),{method:'POST'}).then(r=>r.ok?r.json():null).then(d=>{if(d){setProject(d);projectRef.current=d}})}}}><Undo2 size={18}/></Button><Button className="v-btn p-2" onClick={()=>{if(project){void fetch(api(`/api/projects/${project.id}/redo`),{method:'POST'}).then(r=>r.ok?r.json():null).then(d=>{if(d){setProject(d);projectRef.current=d}})}}}><Redo2 size={18}/></Button><div className="hidden md:block text-[11px] text-emerald-300 px-2">{status}</div><Button className="rounded-xl px-4 py-2 bg-violet-600 hover:bg-violet-500 font-semibold text-sm" onClick={()=>window.open(api(`/api/projects/${project?.id}/render`),'_blank')}>تصدير</Button></div></header>
 <div className="flex-1 min-h-0 flex flex-col lg:flex-row-reverse bg-[#080a0f]"><aside className="lg:w-[78px] shrink-0 order-3 lg:order-none border-t lg:border-t-0 lg:border-l border-white/[.07] bg-[#0d0f14]/96 flex lg:flex-col lg:items-center overflow-x-auto lg:overflow-y-auto v-scroll z-30"><div className="flex lg:flex-col min-w-max lg:min-w-0 w-full lg:w-auto py-1.5 lg:py-2 px-1 gap-1">{Object.entries(categories).map(([id,v])=>{const I=v.icon;return <Button key={id} onClick={()=>openCategory(id as Category)} className={`w-[68px] shrink-0 rounded-xl py-2 text-[10px] flex flex-col items-center gap-1.5 ${panel===id?'bg-violet-500/15 text-violet-300':'text-white/50'}`}><I size={20}/>{v.label}</Button>})}<Button onClick={()=>setPanel('assistant')} className={`w-[68px] shrink-0 rounded-xl py-2 text-[10px] flex flex-col items-center gap-1.5 ${panel==='assistant'?'bg-violet-500/15 text-violet-300':'text-white/50'}`}><Bot size={20}/>المساعد</Button><Button onClick={()=>setPanel('settings')} className={`w-[68px] shrink-0 rounded-xl py-2 text-[10px] flex flex-col items-center gap-1.5 ${panel==='settings'?'bg-violet-500/15 text-violet-300':'text-white/50'}`}><Settings2 size={20}/>الإعدادات</Button></div></aside>
 {panel!=='none'&&panel!=='assistant'&&panel!=='settings'&&panel!=='project'&&<section className="absolute lg:static right-0 top-[64px] bottom-[72px] z-50 w-[min(360px,92vw)] lg:w-[300px] v-card overflow-y-auto v-scroll p-3 m-2 lg:m-3 rounded-2xl"><div className="flex items-center justify-between mb-3"><div className="font-bold">{categories[panel as Category].label}</div><Button onClick={()=>setPanel('none')}><X size={17}/></Button></div><div className="grid grid-cols-2 gap-2">{tools[panel as Category].map(t=>{const I=t.icon;return <Button key={t.id} onClick={()=>toolClick(t)} className="v-btn p-3 min-h-[76px] flex flex-col items-center justify-center gap-2 text-xs hover:bg-white/[.06]"><I size={22}/>{t.label}</Button>})}</div>{panel==='media'&&<div className="mt-4 space-y-2">{project?.assets.map(a=><Button key={a.id} onClick={()=>{const c=clips.find(x=>x.assetId===a.id);if(c){setSelectedClip(c.id);seek(c.startTime)}}} className="w-full flex gap-2 p-2 rounded-xl bg-black/20 border border-white/[.06] text-right"><div className="w-20 h-12 rounded-lg overflow-hidden shrink-0"><ClipThumb asset={a}/></div><div className="min-w-0"><div className="text-xs truncate">{a.name}</div><div className="text-[10px] text-white/35 mt-1">{fmt(a.duration)}</div></div></Button>)}</div>}</section>}
 {panel==='project'&&<section className="absolute z-50 top-[64px] right-2 w-[min(340px,92vw)] v-card rounded-2xl p-4"><div className="flex items-center justify-between mb-4"><h3 className="font-bold">إدارة المشروع</h3><Button onClick={()=>setPanel('none')}><X size={18}/></Button></div><div className="grid grid-cols-2 gap-2"><Button className="v-btn p-4 flex flex-col items-center gap-2 text-xs" onClick={()=>projectFileRef.current?.click()}><FileInput size={20}/>استيراد مشروع</Button><Button className="v-btn p-4 flex flex-col items-center gap-2 text-xs" onClick={exportProject}><Download size={20}/>تصدير مشروع</Button><Button className="v-btn p-4 flex flex-col items-center gap-2 text-xs" onClick={()=>fileRef.current?.click()}><Upload size={20}/>استيراد وسائط</Button><Button className="v-btn p-4 flex flex-col items-center gap-2 text-xs" onClick={()=>setStatus('المشروع محفوظ على الخادم')}><Save size={20}/>حالة الحفظ</Button></div><div className="mt-4 text-xs text-white/45 leading-6">ملف المشروع يحفظ البنية الكاملة للتايملاين والمسارات والمقاطع والإعدادات، ويمكن استعادته كمشروع جديد.</div></section>}
 <main className="flex-1 min-w-0 min-h-0 flex flex-col overflow-hidden"><div className="flex-1 min-h-0 grid grid-cols-1 xl:grid-cols-[minmax(0,1fr)_330px] gap-3 p-2.5 sm:p-3"><section className="v-card rounded-2xl overflow-hidden min-h-[250px] flex flex-col"><div className="flex-1 min-h-0 flex items-center justify-center bg-black/20 p-2 sm:p-3"><div className="relative max-w-full max-h-full aspect-[9/16] h-[min(72vh,760px)] rounded-[18px] overflow-hidden bg-black border border-white/10 shadow-2xl flex items-center justify-center">{activeAsset?.mime.startsWith('audio/')?<div className="w-full h-full flex flex-col items-center justify-center p-8 gap-5"><div className="w-20 h-20 rounded-3xl bg-emerald-500/10 flex items-center justify-center"><Volume2 size={34}/></div><div className="text-sm text-white/80 text-center truncate max-w-[80%]">{activeAsset.name}</div><audio ref={audioRef} src={source(activeAsset)} controls className="w-full"/></div>:activeAsset?.mime.startsWith('image/')?<img src={source(activeAsset)} className="w-full h-full object-contain" style={{filter:`brightness(${100+(active?.brightness||0)*100}%) contrast(${(active?.contrast||1)*100}%) saturate(${(active?.saturation||1)*100}%)`,transform:`rotate(${active?.rotate||0}deg) scaleX(${active?.flipH?-1:1}) scaleY(${active?.flipV?-1:1})`}}/>:activeAsset?<video ref={videoRef} src={source(activeAsset)} muted={active?.volume===0} playsInline className="w-full h-full object-contain" onClick={()=>setPlaying(v=>!v)}/>:<div className="text-sm text-white/35">استورد فيديو للبدء</div>}<div className="absolute bottom-0 left-0 right-0 p-2.5 bg-gradient-to-t from-black/85 to-transparent"><div className="flex items-center justify-between text-[11px] text-white/60"><span>{fmt(playhead)} / {fmt(duration)}</span><span>9:16</span></div><div className="flex items-center justify-center gap-4 mt-1"><Button onClick={()=>seek(playhead-.04)}><ChevronRight size={18}/></Button><Button onClick={()=>setPlaying(v=>!v)} className="w-11 h-11 rounded-full bg-violet-600 flex items-center justify-center">{playing?<Pause size={19}/>:<Play size={19} fill="currentColor"/>}</Button><Button onClick={()=>seek(playhead+.04)}><ChevronLeft size={18}/></Button><Button><Maximize2 size={18}/></Button></div></div></div></div></section>
 <section className="v-card rounded-2xl overflow-hidden hidden xl:flex flex-col min-h-0">{panel==='assistant'?<div className="h-full flex flex-col"><div className="p-4 border-b border-white/[.07] flex items-center justify-between"><b>مساعد Vireon</b><Button onClick={()=>setPanel('none')}><X size={17}/></Button></div><div className="flex-1 overflow-y-auto v-scroll p-3 space-y-2">{messages.length===0&&<div className="p-3 rounded-xl bg-violet-500/10 text-xs text-white/60">اطلب تنفيذ عمليات كاملة مثل القص والتقسيم والسرعة واللون.</div>}{messages.map((m,i)=><div key={i} className={`p-3 rounded-xl text-sm ${m.role==='user'?'bg-white/[.04]':'bg-violet-500/10'}`}>{m.text}</div>)}</div><form onSubmit={e=>{e.preventDefault();const t=assistantText.trim();if(t){setAssistantText('');void command(t)}}} className="p-3 border-t border-white/[.07] flex gap-2"><input value={assistantText} onChange={e=>setAssistantText(e.target.value)} className="flex-1 bg-black/20 border border-white/10 rounded-xl px-3 py-2 text-sm" placeholder="اكتب أمرًا…"/><Button className="bg-violet-600 rounded-xl p-2"><MessageCircle size={17}/></Button></form></div>:panel==='settings'?<div className="p-4"><div className="flex justify-between mb-4"><b>الإعدادات</b><Button onClick={()=>setPanel('none')}><X size={17}/></Button></div><div className="text-xs text-white/45 leading-6">تصميم Mobile First: على الهاتف تكون الأدوات والتايملاين ضمن شريط قابل للتمرير، وعلى الشاشات العريضة تتمدد مساحة العمل تلقائيًا لتملأ الشاشة.</div></div>:<div className="p-4 h-full flex flex-col min-h-0"><div className="flex gap-1 border-b border-white/[.07] pb-2 mb-3">{(['clip','transform','color','audio','ai'] as InspectorTab[]).map(t=><Button key={t} onClick={()=>setTab(t)} className={`px-3 py-2 rounded-lg text-xs ${tab===t?'bg-violet-500/15 text-violet-300':'text-white/45'}`}>{t==='clip'?'أساسي':t==='transform'?'تحويل':t==='color'?'لون':t==='audio'?'صوت':'AI'}</Button>)}</div>{active?<div className="overflow-y-auto v-scroll flex-1">{tab==='clip'&&<div className="space-y-3"><div className="text-sm font-semibold truncate">{active.name}</div><div className="grid grid-cols-2 gap-2"><Button className="v-btn p-3 text-xs" onClick={split}><Split size={17}/> تقسيم</Button><Button className="v-btn p-3 text-xs" onClick={duplicate}><Copy size={17}/> تكرار</Button><Button className="v-btn p-3 text-xs" onClick={remove}><Trash2 size={17}/> حذف</Button><Button className="v-btn p-3 text-xs" onClick={()=>void command('دوّر المقطع 90 درجة')}><RotateCcw size={17}/> دوران</Button></div></div>}{tab==='transform'&&<div className="grid grid-cols-2 gap-2"><Button className="v-btn p-3" onClick={()=>void command('دوّر المقطع 90 درجة')}>90°</Button><Button className="v-btn p-3" onClick={()=>void command('دوّر المقطع 180 درجة')}>180°</Button><Button className="v-btn p-3" onClick={()=>void command('اقلب المقطع أفقيًا')}>قلب أفقي</Button><Button className="v-btn p-3" onClick={()=>void command('اقلب المقطع رأسيًا')}>قلب رأسي</Button></div>}{tab==='color'&&<div>{(['brightness','contrast','saturation'] as const).map(k=><div key={k} className="mb-4"><div className="flex justify-between text-xs text-white/60 mb-2"><span>{k==='brightness'?'الإضاءة':k==='contrast'?'التباين':'التشبع'}</span><span>{Math.round(((active as any)[k]??(k==='brightness'?0:1))*(k==='brightness'?100:100))}%</span></div><input className="w-full accent-violet-500" type="range" min={k==='brightness'?-100:0} max={k==='brightness'?100:300} value={k==='brightness'?Math.round((active.brightness||0)*100):Math.round(((active as any)[k]||1)*100)} onChange={e=>updateClip(active.id,{[k]:Number(e.target.value)/(100)} as any)}/></div>)}</div>}{tab==='audio'&&<div className="space-y-3"><div className="text-xs text-white/60">مستوى الصوت</div><input className="w-full accent-violet-500" type="range" min="0" max="200" value={Math.round((active.volume??1)*100)} onChange={e=>updateClip(active.id,{volume:Number(e.target.value)/100})}/><Button className="v-btn w-full p-3" onClick={()=>updateClip(active.id,{volume:0})}><VolumeX size={17}/> كتم</Button></div>}{tab==='ai'&&<div className="grid grid-cols-2 gap-2">{tools.ai.map(t=>{const I=t.icon;return <Button key={t.id} className="v-btn p-3 min-h-[72px] flex flex-col items-center gap-1 text-xs" onClick={()=>toolClick(t)}><I size={21}/>{t.label}</Button>})}</div>}</div>:<div className="text-sm text-white/35">حدد مقطعًا من التايملاين.</div>}</div></section></div>
 <div className="shrink-0 bg-[#0b0d12] border-t border-white/[.07] px-2.5 py-2 flex items-center gap-2 overflow-x-auto v-scroll">{[{l:'قص',i:Scissors,a:split},{l:'سرعة',i:Gauge,a:()=>setPropertyValue(active?.speed?Math.round(active.speed*100):100)},{l:'انتقال',i:Move3D,a:()=>openCategory('transitions')},{l:'فلاتر',i:Sparkles,a:()=>openCategory('effects')},{l:'تعديل اللون',i:SlidersHorizontal,a:()=>openCategory('adjust')},{l:'AI',i:Bot,a:()=>setPanel('assistant')},{l:'تحريك',i:Move3D,a:()=>void command('أضف حركة كاميرا سينمائية')},{l:'مزج',i:Layers3,a:()=>void command('أضف طبقة تراكب')}].map(x=>{const I=x.i;return <Button key={x.l} onClick={()=>{setBottomTool(x.l);x.a()}} className={`v-btn whitespace-nowrap px-3 py-2 text-xs flex items-center gap-2 ${bottomTool===x.l?'bg-violet-500/15 text-violet-300':''}`}><I size={16}/>{x.l}</Button>})}<div className="mr-auto flex items-center"><Button onClick={()=>setZoom(z=>Math.max(.5,z-.25))}><ZoomOut size={16}/></Button><span className="text-[11px] text-white/40 min-w-10 text-center">{Math.round(zoom*100)}%</span><Button onClick={()=>setZoom(z=>Math.min(4,z+.25))}><ZoomIn size={16}/></Button></div></div>
 <section className="shrink-0 bg-[#0a0c11] border-t border-white/[.07] p-2.5" style={{height:Math.max(240,Math.min(520,timelineHeight))}}><div className="h-full v-card rounded-2xl overflow-hidden flex flex-col"><div className="h-10 shrink-0 border-b border-white/[.07] flex items-center gap-2 px-2.5"><Button className="v-btn p-2" onClick={addTrack}><Plus size={16}/></Button><Button className="v-btn p-2" onClick={()=>setTimelineHeight(h=>Math.min(520,h+40))}><ChevronsDown size={16}/></Button><Button className="v-btn p-2" onClick={()=>setTimelineHeight(h=>Math.max(240,h-40))}><ChevronDown size={16}/></Button><div className="text-[11px] text-white/45 px-2">{status}</div></div><div ref={timelineRef} className="flex-1 min-h-0 overflow-auto v-scroll relative"><div className="relative" style={{width:contentWidth,minWidth:'100%'}}><div className="h-8 sticky top-0 z-20 bg-[#0a0c11]/98 border-b border-white/[.06]" onPointerDown={e=>{const rect=e.currentTarget.getBoundingClientRect();const x=e.clientX-rect.left+(timelineRef.current?.scrollLeft||0)-140;seek(Math.max(0,Math.min(duration,x/timelinePxPerSecond)))}}><div className="absolute inset-y-0 right-0 w-[140px] px-2 pt-2 text-[10px] text-white/35">الوقت</div>{Array.from({length:Math.ceil(duration)+1}).map((_,i)=><span key={i} className="absolute top-2 text-[9px] text-white/30" style={{left:140+i*timelinePxPerSecond}}>{fmt(i).slice(0,5)}</span>)}</div><div className="absolute top-8 bottom-0 w-px bg-violet-400/90 z-30 pointer-events-none" style={{left:140+playhead*timelinePxPerSecond}}><div className="w-3 h-3 rounded-full bg-violet-400 -translate-x-1/2"/></div><div className="timeline-bg">{rows.map(tr=><div key={tr.id} className="h-16 border-b border-white/[.05] flex"><div className="sticky right-0 z-20 w-[140px] shrink-0 bg-[#0b0d12]/98 border-l border-white/[.06] px-2 flex items-center gap-2"><Button onClick={()=>setTrack(tr.id,{visible:tr.visible===false})}>{tr.visible===false?<EyeOff size={14}/>:<Eye size={14}/>}</Button><div className="min-w-0"><div className="text-[10px] text-white/70 truncate">{tr.name}</div><div className="text-[9px] text-white/30">{tr.type}</div></div><div className="mr-auto flex items-center gap-1"><Button onClick={()=>setTrack(tr.id,{muted:!tr.muted})}>{tr.muted?<VolumeX size={13}/>:<Volume2 size={13}/>}</Button><Button onClick={()=>setTrack(tr.id,{locked:!tr.locked})}>{tr.locked?<Lock size={13}/>:<GripVertical size={13}/>}</Button></div></div><div className="relative flex-1">{tr.clips.map(c=>{const selected=c.id===selectedClip;return <div key={c.id} data-clip className={`absolute top-2 h-12 rounded-[10px] overflow-hidden border ${selected?'border-violet-400 ring-2 ring-violet-500/20':'border-white/10'} ${tr.locked?'opacity-55':''}`} style={{left:c.startTime*timelinePxPerSecond,width:Math.max(28,(c.endTime-c.startTime)*timelinePxPerSecond),touchAction:'none'}} onPointerDown={e=>{if(!tr.locked)onTimelinePointerDown(e,c,'move')}}><div className="absolute inset-0"><ClipThumb asset={project?.assets.find(a=>a.id===c.assetId)}/></div><div className="absolute inset-0 bg-gradient-to-t from-black/70 to-transparent"/><div className="absolute inset-x-0 bottom-0 p-1 text-[9px] truncate">{c.text||c.name}</div>{selected&&<><div className="absolute left-0 top-0 bottom-0 w-3 bg-violet-300/60 cursor-ew-resize" onPointerDown={e=>{e.stopPropagation();onTimelinePointerDown(e,c,'left')}}/><div className="absolute right-0 top-0 bottom-0 w-3 bg-violet-300/60 cursor-ew-resize" onPointerDown={e=>{e.stopPropagation();onTimelinePointerDown(e,c,'right')}}/></>}</div>})}</div></div>)}</div></div></div></div></section></main></div></div>}
